const dialog=document.querySelector('#lightbox');document.querySelectorAll('[data-image]').forEach(b=>b.addEventListener('click',()=>{dialog.querySelector('img').src=b.dataset.image;document.querySelector('#download').href=b.dataset.image;dialog.showModal()}));document.querySelector('#close').addEventListener('click',()=>dialog.close());dialog.addEventListener('click',e=>{if(e.target===dialog)dialog.close()});document.querySelectorAll('[data-width]').forEach(b=>b.addEventListener('click',()=>{const w=Number(b.dataset.width),im=document.querySelector('#small-row');im.src=`r1/search-row-${w}.png`;im.style.width=(w*3+48)+'px';document.querySelectorAll('[data-width]').forEach(x=>x.setAttribute('aria-pressed',String(x===b)))}));fetch('data/final-summary.json').then(r=>r.json()).then(s=>{const select=document.querySelector('#cohort');const update=()=>{const values=select.value==='weighted'?s.weighted:s.cohorts[select.value];document.querySelectorAll('[data-metric]').forEach(t=>t.textContent=values[t.dataset.metric].toFixed(2))};select.addEventListener('change',update);update()});